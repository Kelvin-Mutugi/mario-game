git checkout -b feature/score-system
git add .
git commit -m "Add score tracking and UI display"
git push origin feature/score-system
git checkout main
git pull origin main
git checkout -b feature/more-enemies
